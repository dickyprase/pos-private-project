<div class="space-y-4">
    <div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-xs text-stone-500">Kelompok menu POS</p><h2 class="text-xl font-bold">Management Kategori</h2></div><button wire:click="create" class="btn-primary gap-2"><svg viewBox="0 0 24 24" class="size-4" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg>Kategori baru</button></div>
    <section class="rounded-2xl border border-stone-200 bg-white p-3 shadow-sm"><div class="grid gap-2 sm:grid-cols-[1fr_220px]"><div class="relative"><svg viewBox="0 0 24 24" class="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-stone-400" fill="none" stroke="currentColor"><circle cx="11" cy="11" r="7"/><path d="m16.5 16.5 4 4"/></svg><input wire:model.live.debounce.300ms="search" class="field-input pl-10" placeholder="Cari kategori"></div><x-ui.dropdown model="statusFilter" :options="[['value'=>'','label'=>'Semua status'],['value'=>'active','label'=>'Aktif'],['value'=>'inactive','label'=>'Nonaktif']]" /></div></section>
    <div class="table-shell overflow-x-auto"><table class="data-table"><thead><tr><th>Kategori</th><th>Slug</th><th class="text-right">Urutan</th><th class="text-right">Produk</th><th>Status</th><th class="text-right">Aksi</th></tr></thead><tbody>@forelse($categories as $category)<tr wire:key="category-{{ $category->id }}"><td class="font-bold">{{ $category->name }}</td><td class="text-stone-500">{{ $category->slug }}</td><td class="text-right">{{ $category->sort_order }}</td><td class="text-right font-semibold">{{ $category->products_count }}</td><td><span class="status-badge {{ $category->is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-stone-100 text-stone-600' }}">{{ $category->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td><div class="table-actions"><button wire:click="edit({{ $category->id }})" class="table-action-button">Edit</button><button wire:click="toggleActive({{ $category->id }})" class="table-action-button {{ $category->is_active ? 'text-rose-700' : 'text-emerald-700' }}">{{ $category->is_active ? 'Nonaktifkan' : 'Aktifkan' }}</button></div></td></tr>@empty<tr><td colspan="6" class="py-12 text-center text-sm text-stone-500">Kategori tidak ditemukan.</td></tr>@endforelse</tbody></table></div>
    <div data-pagination>{{ $categories->links() }}</div>
    @if($formOpen)<div class="fixed inset-0 z-50 grid place-items-end bg-stone-950/40 backdrop-blur-[2px] sm:place-items-center sm:p-4"><button wire:click="resetForm" class="absolute inset-0 size-full" aria-label="Tutup form kategori"></button><section class="relative w-full max-w-md rounded-t-3xl border-l-4 border-l-brand-500 bg-white shadow-2xl sm:rounded-3xl"><div class="flex items-start gap-3 border-b border-stone-200 p-5"><div class="grid size-11 place-items-center rounded-xl bg-brand-100 text-brand-700"><svg viewBox="0 0 24 24" class="size-5" fill="none" stroke="currentColor"><path d="M4 4h7v7H4zM13 4h7v7h-7zM4 13h7v7H4zM13 13h7v7h-7z"/></svg></div><div class="flex-1"><h3 class="font-bold">{{ $editingId ? 'Edit kategori' : 'Kategori baru' }}</h3><p class="text-sm text-stone-500">Atur nama, urutan, dan status kategori.</p></div><x-ui.modal-close wire:click="resetForm" label="Tutup form kategori" /></div><form wire:submit="save" class="space-y-3 p-5"><label class="field-label">Nama kategori<input wire:model="name" class="field-input mt-1"></label><label class="field-label">Urutan<input wire:model="sortOrder" type="number" min="0" class="field-input mt-1"></label><label class="flex items-center gap-2 text-sm font-semibold"><input wire:model="isActive" type="checkbox"> Aktif di POS</label>@if($errors->any())<p class="text-sm text-rose-600">{{ $errors->first() }}</p>@endif<div class="grid grid-cols-2 gap-2 pt-2"><button type="button" wire:click="resetForm" class="btn-secondary">Batal</button><button class="btn-primary">Simpan</button></div></form></section></div>@endif
</div>
